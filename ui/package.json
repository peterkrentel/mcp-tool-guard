{
  "name": "@mcp-tool-guard/ui",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "pretest": "tsc src/auth-provider.ts --outDir dist-test --module ESNext --target ES2022 --moduleResolution bundler --skipLibCheck --ignoreConfig",
    "test": "node --test src/*.test.mjs"
  },
  "dependencies": {
    "@auth0/auth0-spa-js": "^2.23.0",
    "@azure/msal-browser": "^5.18.0",
    "@mcp-tool-guard/gateway": "*",
    "@mlc-ai/web-llm": "^0.2.84",
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
