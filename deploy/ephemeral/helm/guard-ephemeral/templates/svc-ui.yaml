apiVersion: v1
kind: Service
metadata:
  name: ui
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: ui
  ports:
    - name: http
      port: {{ .Values.ui.service.port }}
      targetPort: 80
